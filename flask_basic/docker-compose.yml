version: '3.2'
services:
  venv:
    build: .
    ports:
      - 80:5000
    volumes:
      - ./app/.:/usr/src/myapp
    working_dir: /usr/src/myapp
    command: sh -c "flask --app app run --host='0.0.0.0'" 
